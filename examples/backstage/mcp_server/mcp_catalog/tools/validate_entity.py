
# Copyright 2025 CNOE
# SPDX-License-Identifier: Apache-2.0
# Generated by CNOE OpenAPI MCP Codegen tool

"""Tools for /validate-entity operations"""

import logging
from typing import Dict, Any
from mcp_catalog.api.client import make_api_request

# Configure logging
logging.basicConfig(level=logging.DEBUG)
logger = logging.getLogger("mcp_tools")


async def validateentity(body_location: str, body_entity: Dict[str, Any]) -> Dict[str, Any]:
    '''
    Validates that the provided entity conforms to the expected schema and contains no errors.

    Args:
        body_location (str): The location identifier for the entity body, used to determine schema context.
        body_entity (Dict[str, Any]): The entity data to be validated against the schema.

    Returns:
        Dict[str, Any]: The JSON response from the API call, containing validation results or error details.

    Raises:
        Exception: If the API request fails or returns an error.

    OpenAPI Specification:
      post:
        summary: Validate an entity against its schema.
        description: Validates that a passed-in entity has no errors in its schema.
        operationId: validateentity
        requestBody:
          required: true
          content:
            application/json:
              schema:
                type: object
                properties:
                  location:
                    type: string
                    description: The location identifier for the entity body.
                  entity:
                    type: object
                    additionalProperties: true
                    description: The entity data to be validated.
                required:
                  - location
                  - entity
        responses:
          '200':
            description: Validation result.
            content:
              application/json:
                schema:
                  type: object
                  description: The validation result or error details.
          '400':
            description: Invalid input or schema validation error.
            content:
              application/json:
                schema:
                  type: object
                  properties:
                    error:
                      type: string
                      description: Error message.
    '''
    logger.debug("Making POST request to /validate-entity")
    params = {}
    
    if body_location is not None:
      params["body_location"] = body_location
    
    if body_entity is not None:
      params["body_entity"] = body_entity
    
    data = None
    

    

    


    
    data = {}

    
    data["location"] = body_location
    

    
    data["entity"] = body_entity
    

    if not data:
        data = None
    success, response = await make_api_request(
        "/validate-entity",
        method="POST",
        params=params,
        data=data
    )
    if not success:
        logger.error(f"Request failed: {response.get('error')}")
        return {"error": response.get('error', 'Request failed')}
    return response
