# Copyright 2025 CNOE
# SPDX-License-Identifier: Apache-2.0
# Generated by CNOE OpenAPI MCP Codegen tool

"""Tools for /pet operations"""

import logging
from typing import Dict, Any, List, Literal
from mcp_petstore.api.client import make_api_request, assemble_nested_body

# Configure logging
logging.basicConfig(level=logging.INFO)
logger = logging.getLogger("mcp_tools")


async def update_pet(
  body_id: int,
  body_name: str,
  body_photoUrls: List[str],
  body_category__id: int = None,
  body_category__name: str = None,
  body_tags: List[Dict[str, Any]] = None,
  body_status: Literal["available", "pending", "sold"] = None,
) -> Any:
  '''
  Update an existing pet by Id.

  Args:
      body_id (int): The unique identifier of the pet to update.
      body_name (str): The name of the pet.
      body_photoUrls (List[str]): A list of photo URLs associated with the pet.
      body_category__id (int, optional): The unique identifier of the pet's category. Defaults to None.
      body_category__name (str, optional): The name of the pet's category. Defaults to None.
      body_tags (List[Dict[str, Any]], optional): A list of tags associated with the pet. Each tag is represented as a dictionary. Defaults to None.
      body_status (Literal['available', 'pending', 'sold'], optional): The status of the pet in the store. Defaults to None.

  Returns:
      Any: The JSON response from the API call containing the updated pet information or an error message.

  Raises:
      Exception: If the API request fails or returns an error.
  '''
  logger.debug("Making PUT request to /pet")

  params = {}
  data = {}

  flat_body = {}
  if body_id is not None:
    flat_body["id"] = body_id
  if body_name is not None:
    flat_body["name"] = body_name
  if body_photoUrls is not None:
    flat_body["photoUrls"] = body_photoUrls
  if body_category__id is not None:
    flat_body["category__id"] = body_category__id
  if body_category__name is not None:
    flat_body["category__name"] = body_category__name
  if body_tags is not None:
    flat_body["tags"] = body_tags
  if body_status is not None:
    flat_body["status"] = body_status
  data = assemble_nested_body(flat_body)

  success, response = await make_api_request("/pet", method="PUT", params=params, data=data)

  if not success:
    logger.error(f"Request failed: {response.get('error')}")
    return {"error": response.get("error", "Request failed")}
  return response


async def add_pet(
  body_id: int,
  body_name: str,
  body_photoUrls: List[str],
  body_category__id: int = None,
  body_category__name: str = None,
  body_tags: List[Dict[str, Any]] = None,
  body_status: Literal["available", "pending", "sold"] = None,
) -> Any:
  '''
  Add a new pet to the store, ensuring no duplicate pet IDs exist.

  Args:
      body_id (int): Unique identifier for the pet.
      body_name (str): Name of the pet.
      body_photoUrls (List[str]): List of photo URLs associated with the pet.
      body_category__id (int, optional): Identifier for the pet's category. Defaults to None.
      body_category__name (str, optional): Name of the pet's category. Defaults to None.
      body_tags (List[Dict[str, Any]], optional): List of tags associated with the pet. Defaults to None.
      body_status (Literal['available', 'pending', 'sold'], optional): Status of the pet in the store. Defaults to None.

  Returns:
      Any: The JSON response from the API call, containing details of the added pet or error information.

  Raises:
      Exception: If the API request fails or returns an error.
  '''
  logger.debug("Making POST request to /pet")

  params = {}
  data = {}

  flat_body = {}
  if body_id is not None:
    flat_body["id"] = body_id
  if body_name is not None:
    flat_body["name"] = body_name
  if body_photoUrls is not None:
    flat_body["photoUrls"] = body_photoUrls
  if body_category__id is not None:
    flat_body["category__id"] = body_category__id
  if body_category__name is not None:
    flat_body["category__name"] = body_category__name
  if body_tags is not None:
    flat_body["tags"] = body_tags
  if body_status is not None:
    flat_body["status"] = body_status
  data = assemble_nested_body(flat_body)

  success, response = await make_api_request("/pet", method="POST", params=params, data=data)

  if not success:
    logger.error(f"Request failed: {response.get('error')}")
    return {"error": response.get("error", "Request failed")}
  return response