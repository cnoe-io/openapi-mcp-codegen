# Copyright 2025 CNOE
# SPDX-License-Identifier: Apache-2.0
# Generated by CNOE OpenAPI MCP Codegen tool

"""Tools for /api/v1/workflows/{namespace}/{name}/stop operations"""

import logging
from typing import Any
from mcp_argo_workflows.api.client import make_api_request, assemble_nested_body

# Configure logging
logging.basicConfig(level=logging.INFO)
logger = logging.getLogger("mcp_tools")


async def put_wf_svc_stop_wf(
  path_namespace: str,
  path_name: str,
  body_message: str = None,
  body_name: str = None,
  body_namespace: str = None,
  body_node_field_selector: str = None,
) -> Any:
  """


  OpenAPI Description:


  Args:

      path_namespace (str): OpenAPI parameter corresponding to 'path_namespace'

      path_name (str): OpenAPI parameter corresponding to 'path_name'

      body_message (str): OpenAPI parameter corresponding to 'body_message'

      body_name (str): OpenAPI parameter corresponding to 'body_name'

      body_namespace (str): OpenAPI parameter corresponding to 'body_namespace'

      body_node_field_selector (str): OpenAPI parameter corresponding to 'body_node_field_selector'


  Returns:
      Any: The JSON response from the API call.

  Raises:
      Exception: If the API request fails or returns an error.
  """
  logger.debug("Making PUT request to /api/v1/workflows/{namespace}/{name}/stop")

  params = {}
  data = {}

  flat_body = {}
  if body_message is not None:
    flat_body["message"] = body_message
  if body_name is not None:
    flat_body["name"] = body_name
  if body_namespace is not None:
    flat_body["namespace"] = body_namespace
  if body_node_field_selector is not None:
    flat_body["node_field_selector"] = body_node_field_selector
  data = assemble_nested_body(flat_body)

  success, response = await make_api_request(f"/api/v1/workflows/{path_namespace}/{path_name}/stop", method="PUT", params=params, data=data)

  if not success:
    logger.error(f"Request failed: {response.get('error')}")
    return {"error": response.get("error", "Request failed")}
  return response
