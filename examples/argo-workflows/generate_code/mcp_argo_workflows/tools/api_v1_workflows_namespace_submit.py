# Copyright 2025 CNOE
# SPDX-License-Identifier: Apache-2.0
# Generated by CNOE OpenAPI MCP Codegen tool

"""Tools for /api/v1/workflows/{namespace}/submit operations"""

import logging
from typing import Dict, Any
from mcp_argo_workflows.api.client import make_api_request, assemble_nested_body

# Configure logging
logging.basicConfig(level=logging.INFO)
logger = logging.getLogger("mcp_tools")


async def post_wf_svc_submit_wf(
  path_namespace: str,
  body_namespace: str = None,
  body_resource_kind: str = None,
  body_resource_name: str = None,
  body_submit_options: Dict[str, Any] = None,
) -> Any:
  """


  OpenAPI Description:


  Args:

      path_namespace (str): OpenAPI parameter corresponding to 'path_namespace'

      body_namespace (str): OpenAPI parameter corresponding to 'body_namespace'

      body_resource_kind (str): OpenAPI parameter corresponding to 'body_resource_kind'

      body_resource_name (str): OpenAPI parameter corresponding to 'body_resource_name'

      body_submit_options (Dict[str, Any]): SubmitOpts are workflow submission options


  Returns:
      Any: The JSON response from the API call.

  Raises:
      Exception: If the API request fails or returns an error.
  """
  logger.debug("Making POST request to /api/v1/workflows/{namespace}/submit")

  params = {}
  data = {}

  flat_body = {}
  if body_namespace is not None:
    flat_body["namespace"] = body_namespace
  if body_resource_kind is not None:
    flat_body["resource_kind"] = body_resource_kind
  if body_resource_name is not None:
    flat_body["resource_name"] = body_resource_name
  if body_submit_options is not None:
    flat_body["submit_options"] = body_submit_options
  data = assemble_nested_body(flat_body)

  success, response = await make_api_request(f"/api/v1/workflows/{path_namespace}/submit", method="POST", params=params, data=data)

  if not success:
    logger.error(f"Request failed: {response.get('error')}")
    return {"error": response.get("error", "Request failed")}
  return response
