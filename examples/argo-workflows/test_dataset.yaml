│   1 Info                                                                                               │
│      • Retrieve system information.                                                                    │
│      • Retrieve user information.                                                                      │
│      • Retrieve system version.                                                                        │
│   2 Cluster Workflow Templates                                                                         │
│      • List all cluster workflow templates.                                                            │
│      • Create a new cluster workflow template with the name my-template.                               │
│      • Retrieve details of the cluster workflow template named my-template.                            │
│      • Update the cluster workflow template named my-template.                                         │
│      • Delete the cluster workflow template named my-template.                                         │
│   3 Cron Workflows                                                                                     │
│      • List all cron workflows in the default namespace.                                               │
│      • Create a new cron workflow in the default namespace with the name my-cron-workflow.             │
│      • Retrieve details of the cron workflow named my-cron-workflow in the default namespace.          │
│      • Update the cron workflow named my-cron-workflow in the default namespace.                       │
│      • Delete the cron workflow named my-cron-workflow in the default namespace.                       │
│      • Resume the cron workflow named my-cron-workflow in the default namespace.                       │
│      • Suspend the cron workflow named my-cron-workflow in the default namespace.                      │
│   4 Event Sources                                                                                      │
│      • List all event sources in the default namespace.                                                │
│      • Create a new event source in the default namespace with the name my-event-source.               │
│      • Retrieve details of the event source named my-event-source in the default namespace.            │
│      • Update the event source named my-event-source in the default namespace.                         │
│      • Delete the event source named my-event-source in the default namespace.                         │
│   5 Sensors                                                                                            │
│      • List all sensors in the default namespace.                                                      │
│      • Create a new sensor in the default namespace with the name my-sensor.                           │
│      • Retrieve details of the sensor named my-sensor in the default namespace.                        │
│      • Update the sensor named my-sensor in the default namespace.                                     │
│      • Delete the sensor named my-sensor in the default namespace.                                     │
│   6 Workflows                                                                                          │
│      • List all workflows in the default namespace.                                                    │
│      • Create a new workflow in the default namespace with the name my-workflow.                       │
│      • Retrieve details of the workflow named my-workflow in the default namespace.                    │
│      • Delete the workflow named my-workflow in the default namespace.                                 │
│      • Resubmit the workflow named my-workflow in the default namespace.                               │
│      • Retry the workflow named my-workflow in the default namespace.                                  │
│      • Suspend the workflow named my-workflow in the default namespace.                                │
│      • Terminate the workflow named my-workflow in the default namespace.                              │
│   7 Artifacts                                                                                          │
│      • Retrieve the output artifact my-artifact from the workflow my-workflow in the default           │
│        namespace.                                                                                      │
│      • Retrieve the input artifact my-artifact from the workflow my-workflow in the default            │
│        namespace.                                                                                      │
│   8 Archived Workflows                                                                                 │
│      • List all archived workflows in the default namespace.                                           │
│      • Retrieve details of the archived workflow with UID 12345.                                       │
│      • Delete the archived workflow with UID 12345.                                                    │
│      • Resubmit the archived workflow with UID 12345 using memoized parameters.                        │
│      • Retry the archived workflow with UID 12345.                                                     │