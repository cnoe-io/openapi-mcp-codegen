# Copyright 2025 CNOE
# SPDX-License-Identifier: Apache-2.0
# Generated by CNOE OpenAPI MCP Codegen tool

"""Tools for /api/v1/rollouts/{namespace}/{rollout}/set/{container}/{image}/{tag} operations"""

import logging
from typing import Any
from mcp_argo_rollouts.api.client import make_api_request, assemble_nested_body

# Configure logging
logging.basicConfig(level=logging.INFO)
logger = logging.getLogger("mcp_tools")


async def put_rollout_svc_set_rollout_image(
    path_namespace: str,
    path_rollout: str,
    path_container: str,
    path_image: str,
    path_tag: str,
    body_rollout: str = None,
    body_container: str = None,
    body_image: str = None,
    body_tag: str = None,
    body_namespace: str = None,
) -> Any:
    """


    OpenAPI Description:


    Args:

        path_namespace (str): OpenAPI parameter corresponding to 'path_namespace'

        path_rollout (str): OpenAPI parameter corresponding to 'path_rollout'

        path_container (str): OpenAPI parameter corresponding to 'path_container'

        path_image (str): OpenAPI parameter corresponding to 'path_image'

        path_tag (str): OpenAPI parameter corresponding to 'path_tag'

        body_rollout (str): OpenAPI parameter corresponding to 'body_rollout'

        body_container (str): OpenAPI parameter corresponding to 'body_container'

        body_image (str): OpenAPI parameter corresponding to 'body_image'

        body_tag (str): OpenAPI parameter corresponding to 'body_tag'

        body_namespace (str): OpenAPI parameter corresponding to 'body_namespace'


    Returns:
        Any: The JSON response from the API call.

    Raises:
        Exception: If the API request fails or returns an error.
    """
    logger.debug("Making PUT request to /api/v1/rollouts/{namespace}/{rollout}/set/{container}/{image}/{tag}")

    params = {}
    data = {}

    flat_body = {}
    if body_rollout is not None:
        flat_body["rollout"] = body_rollout
    if body_container is not None:
        flat_body["container"] = body_container
    if body_image is not None:
        flat_body["image"] = body_image
    if body_tag is not None:
        flat_body["tag"] = body_tag
    if body_namespace is not None:
        flat_body["namespace"] = body_namespace
    data = assemble_nested_body(flat_body)

    success, response = await make_api_request(
        f"/api/v1/rollouts/{path_namespace}/{path_rollout}/set/{path_container}/{path_image}/{path_tag}",
        method="PUT",
        params=params,
        data=data,
    )

    if not success:
        logger.error(f"Request failed: {response.get('error')}")
        return {"error": response.get("error", "Request failed")}
    return response
