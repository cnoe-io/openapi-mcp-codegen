# Copyright 2025 CNOE
# SPDX-License-Identifier: Apache-2.0
# Generated by CNOE OpenAPI MCP Codegen tool

"""Tools for /api/v1/rollouts/{namespace}/{name}/retry operations"""

import logging
from typing import Any
from mcp_argo_rollouts.api.client import make_api_request, assemble_nested_body

# Configure logging
logging.basicConfig(level=logging.INFO)
logger = logging.getLogger("mcp_tools")


async def put_rollout_svc_retry_rollout(path_namespace: str, path_name: str, body_name: str = None, body_namespace: str = None) -> Any:
    """


    OpenAPI Description:


    Args:

        path_namespace (str): OpenAPI parameter corresponding to 'path_namespace'

        path_name (str): OpenAPI parameter corresponding to 'path_name'

        body_name (str): OpenAPI parameter corresponding to 'body_name'

        body_namespace (str): OpenAPI parameter corresponding to 'body_namespace'


    Returns:
        Any: The JSON response from the API call.

    Raises:
        Exception: If the API request fails or returns an error.
    """
    logger.debug("Making PUT request to /api/v1/rollouts/{namespace}/{name}/retry")

    params = {}
    data = {}

    flat_body = {}
    if body_name is not None:
        flat_body["name"] = body_name
    if body_namespace is not None:
        flat_body["namespace"] = body_namespace
    data = assemble_nested_body(flat_body)

    success, response = await make_api_request(
        f"/api/v1/rollouts/{path_namespace}/{path_name}/retry", method="PUT", params=params, data=data
    )

    if not success:
        logger.error(f"Request failed: {response.get('error')}")
        return {"error": response.get("error", "Request failed")}
    return response
