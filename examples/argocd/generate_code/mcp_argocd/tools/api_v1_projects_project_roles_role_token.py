# Copyright 2025 CNOE
# SPDX-License-Identifier: Apache-2.0
# Generated by CNOE OpenAPI MCP Codegen tool

"""Tools for /api/v1/projects/{project}/roles/{role}/token operations"""

import logging
from typing import Any
from mcp_argocd.api.client import make_api_request, assemble_nested_body

# Configure logging
logging.basicConfig(level=logging.INFO)
logger = logging.getLogger("mcp_tools")


async def post_project_svc_new_token(
  path_project: str,
  path_role: str,
  body_description: str = None,
  body_expires_in: int = None,
  body_id: str = None,
  body_project: str = None,
  body_role: str = None,
) -> Any:
  """
  Create a new project token

  OpenAPI Description:


  Args:

      path_project (str): OpenAPI parameter corresponding to 'path_project'

      path_role (str): OpenAPI parameter corresponding to 'path_role'

      body_description (str): OpenAPI parameter corresponding to 'body_description'

      body_expires_in (int): OpenAPI parameter corresponding to 'body_expires_in'

      body_id (str): OpenAPI parameter corresponding to 'body_id'

      body_project (str): OpenAPI parameter corresponding to 'body_project'

      body_role (str): OpenAPI parameter corresponding to 'body_role'


  Returns:
      Any: The JSON response from the API call.

  Raises:
      Exception: If the API request fails or returns an error.
  """
  logger.debug("Making POST request to /api/v1/projects/{project}/roles/{role}/token")

  params = {}
  data = {}

  flat_body = {}
  if body_description is not None:
    flat_body["description"] = body_description
  if body_expires_in is not None:
    flat_body["expires_in"] = body_expires_in
  if body_id is not None:
    flat_body["id"] = body_id
  if body_project is not None:
    flat_body["project"] = body_project
  if body_role is not None:
    flat_body["role"] = body_role
  data = assemble_nested_body(flat_body)

  success, response = await make_api_request(
    f"/api/v1/projects/{path_project}/roles/{path_role}/token", method="POST", params=params, data=data
  )

  if not success:
    logger.error(f"Request failed: {response.get('error')}")
    return {"error": response.get("error", "Request failed")}
  return response
