# Copyright 2025 CNOE
# SPDX-License-Identifier: Apache-2.0
# Generated by CNOE OpenAPI MCP Codegen tool

"""Tools for /api/v1/applications/{name}/revisions/{revision}/metadata operations"""

import logging
from typing import Any
from mcp_argocd.api.client import make_api_request, assemble_nested_body

# Configure logging
logging.basicConfig(level=logging.INFO)
logger = logging.getLogger("mcp_tools")


async def get_application_svc_revision_meta(
  path_name: str,
  path_revision: str,
  param_app_namespace: str = None,
  param_project: str = None,
  param_source_index: int = None,
  param_version_id: int = None,
) -> Any:
  """
  Get the meta-data (author, date, tags, message) for a specific revision of the application

  OpenAPI Description:


  Args:

      path_name (str): the application's name

      path_revision (str): the revision of the app

      param_app_namespace (str): the application's namespace.

      param_project (str): OpenAPI parameter corresponding to 'param_project'

      param_source_index (int): source index (for multi source apps).

      param_version_id (int): versionId from historical data (for multi source apps).


  Returns:
      Any: The JSON response from the API call.

  Raises:
      Exception: If the API request fails or returns an error.
  """
  logger.debug("Making GET request to /api/v1/applications/{name}/revisions/{revision}/metadata")

  params = {}
  data = {}

  if param_app_namespace is not None:
    params["app_namespace"] = str(param_app_namespace).lower() if isinstance(param_app_namespace, bool) else param_app_namespace

  if param_project is not None:
    params["project"] = str(param_project).lower() if isinstance(param_project, bool) else param_project

  if param_source_index is not None:
    params["source_index"] = str(param_source_index).lower() if isinstance(param_source_index, bool) else param_source_index

  if param_version_id is not None:
    params["version_id"] = str(param_version_id).lower() if isinstance(param_version_id, bool) else param_version_id

  flat_body = {}
  data = assemble_nested_body(flat_body)

  success, response = await make_api_request(
    f"/api/v1/applications/{path_name}/revisions/{path_revision}/metadata", method="GET", params=params, data=data
  )

  if not success:
    logger.error(f"Request failed: {response.get('error')}")
    return {"error": response.get("error", "Request failed")}
  return response
