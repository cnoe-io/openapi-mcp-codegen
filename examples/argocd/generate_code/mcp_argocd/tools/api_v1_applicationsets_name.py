# Copyright 2025 CNOE
# SPDX-License-Identifier: Apache-2.0
# Generated by CNOE OpenAPI MCP Codegen tool

"""Tools for /api/v1/applicationsets/{name} operations"""

import logging
from typing import Any
from mcp_argocd.api.client import make_api_request, assemble_nested_body

# Configure logging
logging.basicConfig(level=logging.INFO)
logger = logging.getLogger("mcp_tools")


async def get_application_set_svc_get(path_name: str, param_appset_namespace: str = None) -> Any:
  """
  Get returns an applicationset by name

  OpenAPI Description:


  Args:

      path_name (str): the applicationsets's name

      param_appset_namespace (str): The application set namespace. Default empty is argocd control plane namespace.


  Returns:
      Any: The JSON response from the API call.

  Raises:
      Exception: If the API request fails or returns an error.
  """
  logger.debug("Making GET request to /api/v1/applicationsets/{name}")

  params = {}
  data = {}

  if param_appset_namespace is not None:
    params["appset_namespace"] = str(param_appset_namespace).lower() if isinstance(param_appset_namespace, bool) else param_appset_namespace

  flat_body = {}
  data = assemble_nested_body(flat_body)

  success, response = await make_api_request(f"/api/v1/applicationsets/{path_name}", method="GET", params=params, data=data)

  if not success:
    logger.error(f"Request failed: {response.get('error')}")
    return {"error": response.get("error", "Request failed")}
  return response


async def del_application_set_svc_del(path_name: str, param_appset_namespace: str = None) -> Any:
  """
  Delete deletes an application set

  OpenAPI Description:


  Args:

      path_name (str): OpenAPI parameter corresponding to 'path_name'

      param_appset_namespace (str): The application set namespace. Default empty is argocd control plane namespace.


  Returns:
      Any: The JSON response from the API call.

  Raises:
      Exception: If the API request fails or returns an error.
  """
  logger.debug("Making DELETE request to /api/v1/applicationsets/{name}")

  params = {}
  data = {}

  if param_appset_namespace is not None:
    params["appset_namespace"] = str(param_appset_namespace).lower() if isinstance(param_appset_namespace, bool) else param_appset_namespace

  flat_body = {}
  data = assemble_nested_body(flat_body)

  success, response = await make_api_request(f"/api/v1/applicationsets/{path_name}", method="DELETE", params=params, data=data)

  if not success:
    logger.error(f"Request failed: {response.get('error')}")
    return {"error": response.get("error", "Request failed")}
  return response
