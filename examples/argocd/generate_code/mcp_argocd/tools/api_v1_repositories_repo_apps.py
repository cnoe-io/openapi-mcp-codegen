# Copyright 2025 CNOE
# SPDX-License-Identifier: Apache-2.0
# Generated by CNOE OpenAPI MCP Codegen tool

"""Tools for /api/v1/repositories/{repo}/apps operations"""

import logging
from typing import Any
from mcp_argocd.api.client import make_api_request, assemble_nested_body

# Configure logging
logging.basicConfig(level=logging.INFO)
logger = logging.getLogger("mcp_tools")


async def get_repository_svc_ls_apps(
  path_repo: str, param_revision: str = None, param_app_name: str = None, param_app_project: str = None
) -> Any:
  """
  ListApps returns list of apps in the repo

  OpenAPI Description:


  Args:

      path_repo (str): OpenAPI parameter corresponding to 'path_repo'

      param_revision (str): OpenAPI parameter corresponding to 'param_revision'

      param_app_name (str): OpenAPI parameter corresponding to 'param_app_name'

      param_app_project (str): OpenAPI parameter corresponding to 'param_app_project'


  Returns:
      Any: The JSON response from the API call.

  Raises:
      Exception: If the API request fails or returns an error.
  """
  logger.debug("Making GET request to /api/v1/repositories/{repo}/apps")

  params = {}
  data = {}

  if param_revision is not None:
    params["revision"] = str(param_revision).lower() if isinstance(param_revision, bool) else param_revision

  if param_app_name is not None:
    params["app_name"] = str(param_app_name).lower() if isinstance(param_app_name, bool) else param_app_name

  if param_app_project is not None:
    params["app_project"] = str(param_app_project).lower() if isinstance(param_app_project, bool) else param_app_project

  flat_body = {}
  data = assemble_nested_body(flat_body)

  success, response = await make_api_request(f"/api/v1/repositories/{path_repo}/apps", method="GET", params=params, data=data)

  if not success:
    logger.error(f"Request failed: {response.get('error')}")
    return {"error": response.get("error", "Request failed")}
  return response
