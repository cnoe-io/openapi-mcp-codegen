# Copyright 2025 CNOE
# SPDX-License-Identifier: Apache-2.0
# Generated by CNOE OpenAPI MCP Codegen tool

"""Tools for /api/v1/applications/{name}/events operations"""

import logging
from typing import Any
from mcp_argocd.api.client import make_api_request, assemble_nested_body

# Configure logging
logging.basicConfig(level=logging.INFO)
logger = logging.getLogger("mcp_tools")


async def get_application_svc_events(
  path_name: str,
  param_resource_namespace: str = None,
  param_resource_name: str = None,
  param_resource_uid: str = None,
  param_app_namespace: str = None,
  param_project: str = None,
) -> Any:
  """
  ListResourceEvents returns a list of event resources

  OpenAPI Description:


  Args:

      path_name (str): OpenAPI parameter corresponding to 'path_name'

      param_resource_namespace (str): OpenAPI parameter corresponding to 'param_resource_namespace'

      param_resource_name (str): OpenAPI parameter corresponding to 'param_resource_name'

      param_resource_uid (str): OpenAPI parameter corresponding to 'param_resource_uid'

      param_app_namespace (str): OpenAPI parameter corresponding to 'param_app_namespace'

      param_project (str): OpenAPI parameter corresponding to 'param_project'


  Returns:
      Any: The JSON response from the API call.

  Raises:
      Exception: If the API request fails or returns an error.
  """
  logger.debug("Making GET request to /api/v1/applications/{name}/events")

  params = {}
  data = {}

  if param_resource_namespace is not None:
    params["resource_namespace"] = (
      str(param_resource_namespace).lower() if isinstance(param_resource_namespace, bool) else param_resource_namespace
    )

  if param_resource_name is not None:
    params["resource_name"] = str(param_resource_name).lower() if isinstance(param_resource_name, bool) else param_resource_name

  if param_resource_uid is not None:
    params["resource_uid"] = str(param_resource_uid).lower() if isinstance(param_resource_uid, bool) else param_resource_uid

  if param_app_namespace is not None:
    params["app_namespace"] = str(param_app_namespace).lower() if isinstance(param_app_namespace, bool) else param_app_namespace

  if param_project is not None:
    params["project"] = str(param_project).lower() if isinstance(param_project, bool) else param_project

  flat_body = {}
  data = assemble_nested_body(flat_body)

  success, response = await make_api_request(f"/api/v1/applications/{path_name}/events", method="GET", params=params, data=data)

  if not success:
    logger.error(f"Request failed: {response.get('error')}")
    return {"error": response.get("error", "Request failed")}
  return response
