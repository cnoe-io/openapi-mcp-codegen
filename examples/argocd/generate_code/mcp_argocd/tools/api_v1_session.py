# Copyright 2025 CNOE
# SPDX-License-Identifier: Apache-2.0
# Generated by CNOE OpenAPI MCP Codegen tool

"""Tools for /api/v1/session operations"""

import logging
from typing import Any
from mcp_argocd.api.client import make_api_request, assemble_nested_body

# Configure logging
logging.basicConfig(level=logging.INFO)
logger = logging.getLogger("mcp_tools")


async def post_session_svc_new(body_password: str = None, body_token: str = None, body_username: str = None) -> Any:
  """
  Create a new JWT for authentication and set a cookie if using HTTP

  OpenAPI Description:


  Args:

      body_password (str): OpenAPI parameter corresponding to 'body_password'

      body_token (str): OpenAPI parameter corresponding to 'body_token'

      body_username (str): OpenAPI parameter corresponding to 'body_username'


  Returns:
      Any: The JSON response from the API call.

  Raises:
      Exception: If the API request fails or returns an error.
  """
  logger.debug("Making POST request to /api/v1/session")

  params = {}
  data = {}

  flat_body = {}
  if body_password is not None:
    flat_body["password"] = body_password
  if body_token is not None:
    flat_body["token"] = body_token
  if body_username is not None:
    flat_body["username"] = body_username
  data = assemble_nested_body(flat_body)

  success, response = await make_api_request("/api/v1/session", method="POST", params=params, data=data)

  if not success:
    logger.error(f"Request failed: {response.get('error')}")
    return {"error": response.get("error", "Request failed")}
  return response


async def del_session_svc_del() -> Any:
  """
  Delete an existing JWT cookie if using HTTP

  OpenAPI Description:


  Args:


  Returns:
      Any: The JSON response from the API call.

  Raises:
      Exception: If the API request fails or returns an error.
  """
  logger.debug("Making DELETE request to /api/v1/session")

  params = {}
  data = {}

  flat_body = {}
  data = assemble_nested_body(flat_body)

  success, response = await make_api_request("/api/v1/session", method="DELETE", params=params, data=data)

  if not success:
    logger.error(f"Request failed: {response.get('error')}")
    return {"error": response.get("error", "Request failed")}
  return response
