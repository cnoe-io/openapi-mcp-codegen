# Copyright 2025 CNOE
# SPDX-License-Identifier: Apache-2.0
# Generated by CNOE OpenAPI MCP Codegen tool

"""Tools for /api/v1/applications/manifestsWithFiles operations"""

import logging
from typing import Any
from mcp_argocd.api.client import make_api_request, assemble_nested_body

# Configure logging
logging.basicConfig(level=logging.INFO)
logger = logging.getLogger("mcp_tools")


async def post_application_svc_files(
  body_chunk_chunk: str = None,
  body_query_app_namespace: str = None,
  body_query_checksum: str = None,
  body_query_name: str = None,
  body_query_project: str = None,
) -> Any:
  """
  GetManifestsWithFiles returns application manifests using provided files to generate them

  OpenAPI Description:


  Args:

      body_chunk_chunk (str): OpenAPI parameter corresponding to 'body_chunk_chunk'

      body_query_app_namespace (str): OpenAPI parameter corresponding to 'body_query_app_namespace'

      body_query_checksum (str): OpenAPI parameter corresponding to 'body_query_checksum'

      body_query_name (str): OpenAPI parameter corresponding to 'body_query_name'

      body_query_project (str): OpenAPI parameter corresponding to 'body_query_project'


  Returns:
      Any: The JSON response from the API call.

  Raises:
      Exception: If the API request fails or returns an error.
  """
  logger.debug("Making POST request to /api/v1/applications/manifestsWithFiles")

  params = {}
  data = {}

  flat_body = {}
  if body_chunk_chunk is not None:
    flat_body["chunk_chunk"] = body_chunk_chunk
  if body_query_app_namespace is not None:
    flat_body["query_app_namespace"] = body_query_app_namespace
  if body_query_checksum is not None:
    flat_body["query_checksum"] = body_query_checksum
  if body_query_name is not None:
    flat_body["query_name"] = body_query_name
  if body_query_project is not None:
    flat_body["query_project"] = body_query_project
  data = assemble_nested_body(flat_body)

  success, response = await make_api_request("/api/v1/applications/manifestsWithFiles", method="POST", params=params, data=data)

  if not success:
    logger.error(f"Request failed: {response.get('error')}")
    return {"error": response.get("error", "Request failed")}
  return response
