# Copyright 2025 CNOE
# SPDX-License-Identifier: Apache-2.0
# Generated by CNOE OpenAPI MCP Codegen tool

"""Tools for /api/v1/projects operations"""

import logging
from typing import Dict, Any
from mcp_argocd.api.client import make_api_request, assemble_nested_body

# Configure logging
logging.basicConfig(level=logging.INFO)
logger = logging.getLogger("mcp_tools")


async def get_project_svc_ls(param_name: str = None) -> Any:
  """
  List returns list of projects

  OpenAPI Description:


  Args:

      param_name (str): OpenAPI parameter corresponding to 'param_name'


  Returns:
      Any: The JSON response from the API call.

  Raises:
      Exception: If the API request fails or returns an error.
  """
  logger.debug("Making GET request to /api/v1/projects")

  params = {}
  data = {}

  if param_name is not None:
    params["name"] = str(param_name).lower() if isinstance(param_name, bool) else param_name

  flat_body = {}
  data = assemble_nested_body(flat_body)

  success, response = await make_api_request("/api/v1/projects", method="GET", params=params, data=data)

  if not success:
    logger.error(f"Request failed: {response.get('error')}")
    return {"error": response.get("error", "Request failed")}
  return response


async def post_project_svc_new(
  body_project_metadata: Dict[str, Any] = None,
  body_project_spec: Dict[str, Any] = None,
  body_project_status_jwt_tokens_by_role: Dict[str, Any] = None,
  body_upsert: bool = None,
) -> Any:
  """
      Create a new project

      OpenAPI Description:


      Args:

          body_project_metadata (Dict[str, Any]): ObjectMeta is metadata that all persisted resources must have, which includes all objects
  users must create.

          body_project_spec (Dict[str, Any]): Request body as dictionary. Contains 14 nested properties. See OpenAPI schema for detailed structure.

          body_project_status_jwt_tokens_by_role (Dict[str, Any]): OpenAPI parameter corresponding to 'body_project_status_jwt_tokens_by_role'

          body_upsert (bool): OpenAPI parameter corresponding to 'body_upsert'


      Returns:
          Any: The JSON response from the API call.

      Raises:
          Exception: If the API request fails or returns an error.
  """
  logger.debug("Making POST request to /api/v1/projects")

  params = {}
  data = {}

  flat_body = {}
  if body_project_metadata is not None:
    flat_body["project_metadata"] = body_project_metadata
  if body_project_spec is not None:
    flat_body["project_spec"] = body_project_spec
  if body_project_status_jwt_tokens_by_role is not None:
    flat_body["project_status_jwt_tokens_by_role"] = body_project_status_jwt_tokens_by_role
  if body_upsert is not None:
    flat_body["upsert"] = body_upsert
  data = assemble_nested_body(flat_body)

  success, response = await make_api_request("/api/v1/projects", method="POST", params=params, data=data)

  if not success:
    logger.error(f"Request failed: {response.get('error')}")
    return {"error": response.get("error", "Request failed")}
  return response
