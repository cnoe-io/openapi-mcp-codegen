# Copyright 2025 CNOE
# SPDX-License-Identifier: Apache-2.0
# Generated by CNOE OpenAPI MCP Codegen tool

"""Tools for /api/v1/applications/{name}/resource/actions/v2 operations"""

import logging
from typing import Dict, Any, List
from mcp_argocd.api.client import make_api_request, assemble_nested_body

# Configure logging
logging.basicConfig(level=logging.INFO)
logger = logging.getLogger("mcp_tools")


async def post_application_svc_v2(
  path_name: str,
  body_action: str = None,
  body_app_namespace: str = None,
  body_group: str = None,
  body_kind: str = None,
  body_name: str = None,
  body_namespace: str = None,
  body_project: str = None,
  body_resource_action_parameters: List[Dict[str, Any]] = None,
  body_resource_name: str = None,
  body_version: str = None,
) -> Any:
  """
  RunResourceActionV2 runs a resource action with parameters

  OpenAPI Description:


  Args:

      path_name (str): OpenAPI parameter corresponding to 'path_name'

      body_action (str): OpenAPI parameter corresponding to 'body_action'

      body_app_namespace (str): OpenAPI parameter corresponding to 'body_app_namespace'

      body_group (str): OpenAPI parameter corresponding to 'body_group'

      body_kind (str): OpenAPI parameter corresponding to 'body_kind'

      body_name (str): OpenAPI parameter corresponding to 'body_name'

      body_namespace (str): OpenAPI parameter corresponding to 'body_namespace'

      body_project (str): OpenAPI parameter corresponding to 'body_project'

      body_resource_action_parameters (List[Dict[str, Any]]): OpenAPI parameter corresponding to 'body_resource_action_parameters'

      body_resource_name (str): OpenAPI parameter corresponding to 'body_resource_name'

      body_version (str): OpenAPI parameter corresponding to 'body_version'


  Returns:
      Any: The JSON response from the API call.

  Raises:
      Exception: If the API request fails or returns an error.
  """
  logger.debug("Making POST request to /api/v1/applications/{name}/resource/actions/v2")

  params = {}
  data = {}

  flat_body = {}
  if body_action is not None:
    flat_body["action"] = body_action
  if body_app_namespace is not None:
    flat_body["app_namespace"] = body_app_namespace
  if body_group is not None:
    flat_body["group"] = body_group
  if body_kind is not None:
    flat_body["kind"] = body_kind
  if body_name is not None:
    flat_body["name"] = body_name
  if body_namespace is not None:
    flat_body["namespace"] = body_namespace
  if body_project is not None:
    flat_body["project"] = body_project
  if body_resource_action_parameters is not None:
    flat_body["resource_action_parameters"] = body_resource_action_parameters
  if body_resource_name is not None:
    flat_body["resource_name"] = body_resource_name
  if body_version is not None:
    flat_body["version"] = body_version
  data = assemble_nested_body(flat_body)

  success, response = await make_api_request(
    f"/api/v1/applications/{path_name}/resource/actions/v2", method="POST", params=params, data=data
  )

  if not success:
    logger.error(f"Request failed: {response.get('error')}")
    return {"error": response.get("error", "Request failed")}
  return response
