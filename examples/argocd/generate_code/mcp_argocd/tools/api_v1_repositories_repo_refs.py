# Copyright 2025 CNOE
# SPDX-License-Identifier: Apache-2.0
# Generated by CNOE OpenAPI MCP Codegen tool

"""Tools for /api/v1/repositories/{repo}/refs operations"""

import logging
from typing import Any
from mcp_argocd.api.client import make_api_request, assemble_nested_body

# Configure logging
logging.basicConfig(level=logging.INFO)
logger = logging.getLogger("mcp_tools")


async def get_repository_svc_ls_refs(path_repo: str, param_force_refresh: bool = False, param_app_project: str = None) -> Any:
  """


  OpenAPI Description:


  Args:

      path_repo (str): Repo URL for query

      param_force_refresh (bool): Whether to force a cache refresh on repo's connection state.

      param_app_project (str): App project for query.


  Returns:
      Any: The JSON response from the API call.

  Raises:
      Exception: If the API request fails or returns an error.
  """
  logger.debug("Making GET request to /api/v1/repositories/{repo}/refs")

  params = {}
  data = {}

  if param_force_refresh is not None:
    params["force_refresh"] = str(param_force_refresh).lower() if isinstance(param_force_refresh, bool) else param_force_refresh

  if param_app_project is not None:
    params["app_project"] = str(param_app_project).lower() if isinstance(param_app_project, bool) else param_app_project

  flat_body = {}
  data = assemble_nested_body(flat_body)

  success, response = await make_api_request(f"/api/v1/repositories/{path_repo}/refs", method="GET", params=params, data=data)

  if not success:
    logger.error(f"Request failed: {response.get('error')}")
    return {"error": response.get("error", "Request failed")}
  return response
