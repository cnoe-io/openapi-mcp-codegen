# Copyright 2025 CNOE
# SPDX-License-Identifier: Apache-2.0
# Generated by CNOE OpenAPI MCP Codegen tool

"""Tools for /api/v1/account/password operations"""

import logging
from typing import Any
from mcp_argocd.api.client import make_api_request, assemble_nested_body

# Configure logging
logging.basicConfig(level=logging.INFO)
logger = logging.getLogger("mcp_tools")


async def put_acct_svc_upd_password(body_current_password: str = None, body_name: str = None, body_new_password: str = None) -> Any:
  """
  UpdatePassword updates an account's password to a new value

  OpenAPI Description:


  Args:

      body_current_password (str): OpenAPI parameter corresponding to 'body_current_password'

      body_name (str): OpenAPI parameter corresponding to 'body_name'

      body_new_password (str): OpenAPI parameter corresponding to 'body_new_password'


  Returns:
      Any: The JSON response from the API call.

  Raises:
      Exception: If the API request fails or returns an error.
  """
  logger.debug("Making PUT request to /api/v1/account/password")

  params = {}
  data = {}

  flat_body = {}
  if body_current_password is not None:
    flat_body["current_password"] = body_current_password
  if body_name is not None:
    flat_body["name"] = body_name
  if body_new_password is not None:
    flat_body["new_password"] = body_new_password
  data = assemble_nested_body(flat_body)

  success, response = await make_api_request("/api/v1/account/password", method="PUT", params=params, data=data)

  if not success:
    logger.error(f"Request failed: {response.get('error')}")
    return {"error": response.get("error", "Request failed")}
  return response
