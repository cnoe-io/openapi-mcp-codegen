# Copyright 2025 CNOE
# SPDX-License-Identifier: Apache-2.0
# Generated by CNOE OpenAPI MCP Codegen tool

"""Tools for /api/v1/repocreds/{creds.url} operations"""

import logging
from typing import Dict, Any
from mcp_argocd.api.client import make_api_request, assemble_nested_body

# Configure logging
logging.basicConfig(level=logging.INFO)
logger = logging.getLogger("mcp_tools")


async def put_repo_creds_credentials(path_creds_url: str, body: Dict[str, Any] = None) -> Any:
  """
  UpdateRepositoryCredentials updates a repository credential set

  OpenAPI Description:


  Args:

      path_creds_url (str): URL is the URL to which these credentials match

      body (Dict[str, Any]): Request body as dictionary. Contains 19 nested properties. See OpenAPI schema for detailed structure.


  Returns:
      Any: The JSON response from the API call.

  Raises:
      Exception: If the API request fails or returns an error.
  """
  logger.debug("Making PUT request to /api/v1/repocreds/{creds.url}")

  params = {}
  data = {}

  flat_body = {}
  data = assemble_nested_body(flat_body)

  success, response = await make_api_request(f"/api/v1/repocreds/{path_creds_url}", method="PUT", params=params, data=data)

  if not success:
    logger.error(f"Request failed: {response.get('error')}")
    return {"error": response.get("error", "Request failed")}
  return response
