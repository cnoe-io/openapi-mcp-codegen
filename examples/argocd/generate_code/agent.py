# Copyright 2025 CNOE
# SPDX-License-Identifier: Apache-2.0
# Generated by CNOE OpenAPI MCP Codegen tool

"""LangGraph React-agent wrapper for the generated MCP server."""

import asyncio
import importlib.util
import logging
import os
from pathlib import Path
from dotenv import load_dotenv

from datetime import datetime, timezone
from langchain_core.tools import tool

from langgraph.prebuilt import create_react_agent
from langgraph.checkpoint.memory import MemorySaver
from langchain_mcp_adapters.client import MultiServerMCPClient
from cnoe_agent_utils import LLMFactory
from langfuse import get_client

load_dotenv()  # makes values from .env available via os.getenv

logger = logging.getLogger(__name__)


# Default system prompt (auto-generated by the generator)
DEFAULT_SYSTEM_PROMPT = r"""You are an expert ArgoCD assistant that helps users manage, deploy, and monitor applications using GitOps practices in Kubernetes.

# CORE CAPABILITIES
You can help with:
- 🚀 **Application Management**: Create, deploy, sync, and manage applications across clusters
- 📦 **Repository Management**: Configure Git repositories and Helm chart sources
- 🏗️ **Cluster Management**: Manage destination Kubernetes clusters for deployments
- 👥 **Project Management**: Organize applications with RBAC policies and governance
- 🔐 **Security & Access**: Manage certificates, user accounts, and authentication
- 📊 **Monitoring**: View application health, sync status, and deployment history
- 🔍 **Troubleshooting**: Debug sync failures, investigate issues, and track changes

# INTERACTION GUIDELINES
🎯 **Be Specific**: When asking for application operations, provide:
- Application names and projects
- Target namespaces and clusters
- Git repository URLs and paths
- Specific sync or deployment parameters

💡 **Example Requests**:
- "List all applications in project 'platform-services'"
- "Sync application 'web-frontend' to the latest commit"
- "Create a new application from repository 'https://github.com/my-org/k8s-manifests'"
- "Check the health status of application 'api-gateway'"
- "Add a new Git repository to ArgoCD with private key authentication"
- "Get sync history for application 'database' in the last week"

# APPLICATION CREATION TIPS
When creating applications, I'll guide you through:
- Application specification (source, destination, project)
- Git repository configuration and credentials
- Sync policies and automation settings
- Health checks and sync strategies
- Parameter overrides and values files

# COMMON PATTERNS
- For **new applications**: I'll ask for Git source, destination cluster, and project assignment
- For **monitoring**: I'll help check sync status, health, and deployment progress
- For **troubleshooting**: I'll examine sync errors, resource events, and configuration issues
- For **repository management**: I'll help configure credentials, certificates, and access

# GITOPS BEST PRACTICES
I'll help you follow GitOps principles:
- Declarative application definitions stored in Git
- Automated synchronization between Git and clusters
- Centralized application lifecycle management
- RBAC and policy enforcement through projects
- Audit trails and change tracking

Ask me anything about ArgoCD - I'm here to help you implement GitOps and manage your Kubernetes applications!"""

# Locate the generated MCP server module
spec = importlib.util.find_spec("mcp_argocd.server")
if not spec or not spec.origin:
    raise ImportError("Cannot find mcp_argocd.server module")
server_path = str(Path(spec.origin).resolve())


@tool
def get_current_time() -> str:
    """Return the current date-time in ISO-8601 format."""
    return datetime.now().isoformat()


@tool
def iso8601_to_unix(iso_str: str) -> str:
    """
    Convert an ISO-8601 datetime string to a Unix timestamp (seconds since epoch).
    If no timezone is present, assume UTC.
    """
    s = iso_str.strip()
    # Support common variations: with/without 'Z', with offset, with space instead of 'T'
    # Normalize space to 'T' to help fromisoformat
    if " " in s and "T" not in s:
        s = s.replace(" ", "T")

    # Handle trailing 'Z' (UTC)
    if s.endswith("Z"):
        s = s[:-1] + "+00:00"

    try:
        dt = datetime.fromisoformat(s)
    except ValueError:
        # Fallback: try parsing without separators or with milliseconds
        raise ValueError(f"Invalid ISO-8601 datetime string: {iso_str}")

    if dt.tzinfo is None:
        dt = dt.replace(tzinfo=timezone.utc)

    return str(int(dt.timestamp()))


async def create_agent(prompt: str | None = None, response_format=None):
    """
    Spin-up the MCP server as a subprocess via MultiServerMCPClient and build
    and returns the LangGraph agent **and its tool list**.
    """
    memory = MemorySaver()

    if prompt is None:
        prompt = DEFAULT_SYSTEM_PROMPT  # ← use literal string, no fallback

    api_url = os.getenv("ARGOCD_API_URL")
    api_token = os.getenv("ARGOCD_TOKEN")
    if not api_url or not api_token:
        raise ValueError("Set ARGOCD_API_URL and ARGOCD_TOKEN env vars")

    client = MultiServerMCPClient(
        {
            "argocd": {
                "command": "uv",
                "args": ["run", server_path],
                "env": {
                    "ARGOCD_API_URL": api_url,
                    "ARGOCD_TOKEN": api_token,
                },
                "transport": "stdio",
            }
        }
    )
    mcp_tools = await client.get_tools()
    try:
        get_client().update_current_trace(tags=["argocd-agent"])
    except Exception:
        pass
    tools = mcp_tools + [get_current_time, iso8601_to_unix]
    # Attach Langfuse callback handler so LangChain/LLM/tool calls are traced
    try:
        get_client().update_current_trace(tags=["argocd-agent"])
    except Exception:
        pass
    agent = create_react_agent(
        LLMFactory().get_llm(),
        tools=tools,
        checkpointer=memory,
        prompt=prompt,
        response_format=response_format,
    )
    return agent, tools  # also return raw tool list for evaluators


# Convenience synchronous wrapper
def create_agent_sync(prompt: str | None = None, response_format=None):
    agent, tools = asyncio.run(create_agent(prompt, response_format))
    try:
        get_client().flush()
    except Exception:
        pass
    return agent, tools
