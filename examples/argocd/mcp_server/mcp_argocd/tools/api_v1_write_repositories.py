# Copyright 2025 CNOE
# SPDX-License-Identifier: Apache-2.0
# Generated by CNOE OpenAPI MCP Codegen tool

"""Tools for /api/v1/write-repositories operations"""

import logging
from typing import Dict, Any
from agent_argocd.protocol_bindings.mcp_server.mcp_argocd.api.client import make_api_request

# Configure logging
logging.basicConfig(level=logging.DEBUG)
logger = logging.getLogger("mcp_tools")


async def repository_service__list_write_repositories(
    param_repo: str = None, param_forceRefresh: str = None, param_appProject: str = None
) -> Dict[str, Any]:
    '''
    ListWriteRepositories gets a list of all configured write repositories.

    Args:
        param_repo (str, optional): Repo URL for query. Defaults to None.
        param_forceRefresh (str, optional): Whether to force a cache refresh on repo's connection state. Defaults to None.
        param_appProject (str, optional): App project for query. Defaults to None.

    Returns:
        Dict[str, Any]: The JSON response from the API call containing the list of write repositories.

    Raises:
        Exception: If the API request fails or returns an error.
    '''
    logger.debug("Making GET request to /api/v1/write-repositories")

    params = {}
    data = {}

    params["repo"] = param_repo
    params["forceRefresh"] = param_forceRefresh
    params["appProject"] = param_appProject

    success, response = await make_api_request("/api/v1/write-repositories", method="GET", params=params, data=data)

    if not success:
        logger.error(f"Request failed: {response.get('error')}")
        return {"error": response.get("error", "Request failed")}
    return response


async def repository_service__create_write_repository(
    param_upsert: str = None, param_credsOnly: str = None
) -> Dict[str, Any]:
    '''
    Create a new write repository configuration.

    Args:
        param_upsert (str, optional): Whether to create in upsert mode. Defaults to None.
        param_credsOnly (str, optional): Whether to operate on credential set instead of repository. Defaults to None.

    Returns:
        Dict[str, Any]: The JSON response from the API call.

    Raises:
        Exception: If the API request fails or returns an error.
    '''
    logger.debug("Making POST request to /api/v1/write-repositories")

    params = {}
    data = {}

    params["upsert"] = param_upsert
    params["credsOnly"] = param_credsOnly

    success, response = await make_api_request("/api/v1/write-repositories", method="POST", params=params, data=data)

    if not success:
        logger.error(f"Request failed: {response.get('error')}")
        return {"error": response.get("error", "Request failed")}
    return response